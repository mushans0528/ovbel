# 05 — 验收清单

## 数据与内容
- [x] `lib/products.ts` 中 `impact-bed` EN 条目存在且字段完整。
- [x] `zhProducts["impact-bed"]` 中文条目存在且与 EN 信息对等。
- [x] 未编造参数、认证、价格、库存、应用承诺。
- [x] 缓冲条尺寸（50/75/100 mm 高、100 mm 宽、1200–2000 mm 长）、UHMWPE 12.5 mm 厚度、T-bolt M16×45 固定套件、产品编号均来自 h-col-128 页面规格表图片。
- [x] `sourceUrl` 指向 https://www.respowerbelt.com/h-col-128.html。

## 素材
- [x]（v2）4 张详情页图库照片均为 OVBEL 用户提供的实物 PNG，已转换 webp 至 `public/products/impact-bed/`，命名严格匹配 1→2→3→4。
- [x]（v2）`lib/products.ts` 中 EN/ZH `media.gallery` 同步更新为 4 张；新增 EN + ZH 各 4 条 `galleryAlt`。
- [x]（v2）旧版 8 张图（4 张 gallery + 4 张既有 images）已被新图覆盖或清理，目录内不再有 orphan 文件。
- [x] 详情页图库图、构造图、技术图三类资产全部就绪，无 404 引用。
- [x] 所有图片 `alt` 文本已在中英文文案/组件中定义。
- [x] 无放大镜装饰图标。

## 页面渲染
- [x] 图库、简介、优势、应用、规格、结构、技术文档、询盘清单均按需展示。
- [x] 本产品无型号/花纹分支，故未展示 patterns 区块。
- [x] 语言切换保持当前产品 slug 与查询参数。
- [x] 分类页、产品卡片、相关产品、询盘预填自动同步。

## SEO
- [x] canonical / hreflang / OG / Product JSON-LD 由现有组件自动输出。
- [x] `seo.title` / `seo.description` 已填写。
- [x] JSON-LD 不含 `offers`、`review`、`aggregateRating`。

## 构建与质量
- [x] `tsc --noEmit` 通过（0 错误）。
- [x] `next build` 成功，静态页面数保持（未新增产品 slug，仅更新现有产品媒体）。
- [x] 未修改任何代理 / middleware 文件。
- [x] 文档目录 `docs/product-pages/impact-bed/` 下 6 份文件完整。

## 推送
- [x] 通过 GitHub Git Database API 推送到 `origin/main`。
- [x] 提交哈希、验证结果已记录。

## 本次（v2，2026-09-01）增量变更

- EN `media.gallery`：8 项 → 4 项；`media.galleryAlt`：空 → 4 条 EN。
- ZH `impact-bed`：新增 `media` 块（含 4 项 gallery + 4 条 ZH `galleryAlt` + `constructionImage` + `technicalImages`），与 EN 等价。
- `constructionImage` 与 `technicalImages` 保持不变（均仍指向原 v1 webp/png，未触及本轮用户上传范围）。
- 物理资产：
  - 新增 `/public/products/impact-bed/cover.webp`, `gallery-01.webp`, `gallery-02.webp`, `gallery-03.webp`（来自用户 4 张 PNG）。
  - 删除 6 张孤儿旧 webp（`impact-bars-red.webp`、`impact-bars-blue-trough.webp`、`impact-bars-red-stack.webp` 在 products/；`impact-bed-installed.webp`、`impact-bed-set.webp`、`impact-bar-black.webp` 在 images/）。
- 未修改 `description` / `applicationIntro` / `features` / `specifications` / `precautions` / `inquiryChecklist` / `seo` 等文案字段。
- 未修改 slug、category、sourceUrl、layers 结构。
- 未引入新组件、未引入新路由、未改 proxy / middleware。

## 仍待业务确认（未在页面展示）
- 缓冲条橡胶硬度、UHMWPE 牌号与颜色选项。
- 铝/钢型材的具体牌号与截面尺寸。
- 床体支撑架材质、调节行程与安装孔位。
- 适用认证（CE、ATEX、MSHA 等）。
- 产品编号与长度的完整对应关系及可定制范围。
- 最小/推荐带轮直径、最大允许带速、工作温度范围。
- 库存、交期、MOQ、价格、保修。
- 用户提供的 4 张照片是否需要替换为 OVBEL 现场拍摄版本（本次明确为用户提供 = 已授权使用范围）。
