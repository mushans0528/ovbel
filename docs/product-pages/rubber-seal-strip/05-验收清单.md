# 05 — 验收清单

## 数据与内容

- [x] `lib/products.ts` 中 `rubber-seal-strip` EN 条目存在且字段完整。
- [x] `zhProducts["rubber-seal-strip"]` 中文条目存在且与 EN 信息对等。
- [x] 未编造参数、认证、价格、库存、应用承诺。
- [x] 材料（EPDM）、颜色（黑色）、硬度（60–65 SHA）、尺寸（可定制）、应用领域与功能描述均来自 h-col-154 页面参数表或图片。
- [x] `sourceUrl` 指向 https://www.respowerbelt.com/h-col-154.html。

## 素材

- [x] 5 张新图片均来自 OVBEL 自有 CDN，已下载到 `public/products/rubber-seal-strip/`。
- [x] 图片已转码为 webp，文件命名与内容一致。
- [x] 所有图片 `alt` 文本已在中英文文案/组件中定义。
- [x] 无放大镜装饰图标。

## 页面渲染

- [x] 图库、简介、优势、应用、规格、技术文档、储运须知、询盘清单均按需展示。
- [x] 本产品无结构分层（`layers`）与花纹分支（`patterns`），故未展示 construction / patterns 区块。
- [x] 语言切换保持当前产品 slug 与查询参数。
- [x] 分类页、产品卡片、相关产品、询盘预填自动同步。

## SEO

- [x] canonical / hreflang / OG / Product JSON-LD 由现有组件自动输出。
- [x] `seo.title` / `seo.description` 已填写。
- [x] JSON-LD 不含 `offers`、`review`、`aggregateRating`。

## 构建与质量

- [x] `tsc --noEmit` 通过（0 错误）。
- [x] `next build` 成功，静态页面生成 39 页（新增 1 个产品 slug）。
- [x] 未修改任何代理 / middleware 文件。
- [x] 文档目录 `docs/product-pages/rubber-seal-strip/` 下 6 份文件完整。

## 推送

- [x] 通过 GitHub Git Database API 推送到 `origin/main`。
- [x] 提交哈希、验证结果已记录。

## 仍待业务确认（未在页面展示）

- 具体尺寸系列表（U 型槽内宽 × 高度 × 壁厚、泡型边半径、夹持金属板厚度范围等）。
- EPDM 材料的具体牌号、硬度扩展范围。
- 耐温范围、耐臭氧/耐油等级、阻燃等级。
- 压缩永久变形率、拉伸强度、断裂伸长率等物理性能。
- 颜色选项（页面仅给出黑色）。
- FDA、ROHS、REACH 等认证的具体标准号与适用范围。
- 单卷/单根长度、MOQ、价格、交期、包装方式与保修。
