# 03-实现规格 — Industrial Rubber Sheet / 工业橡胶板

## 数据模型

- 复用 `lib/products.ts` 中的 `Product` 类型。
- 未新增 slug；在现有 `industrial-rubber-sheet` 占位条目上补全字段。
- 关键字段：
  - `category`: `"rubber-products-hoses"`
  - `description`, `applicationIntro`, `featureHeading`
  - `features`, `applications`, `specifications`, `precautions`, `inquiryChecklist`
  - `seo.title`, `seo.description`
  - `sourceUrl`: `https://www.respowerbelt.com/h-col-155.html`
  - `media`: `{ cover, gallery[], technicalImages[] }`

## 路由与组件

- 复用现有动态路由 `[locale]/[[...path]]/page.tsx`。
- 复用 `ProductDetailPage` 组件；不新增页面组件。
- 图库使用 `ProductGallery`（灯箱、键盘 Esc/左右、无放大镜图标）。
- 技术图使用 `ZoomableImage` 同样支持全屏查看。
- 询盘清单自动传入 `InquiryForm` 作为预填项。

## 新增/修改文件

| 文件/目录 | 说明 |
|---|---|
| `lib/products.ts` | 补全 `industrial-rubber-sheet` EN 与 ZH 条目。 |
| `public/products/industrial-rubber-sheet/` | 新增 5 张 webp 图片（cover + 3 gallery + 1 参数表）。 |
| `docs/product-pages/industrial-rubber-sheet/` | 新增 6 份文档。 |

## 未新增架构

- 未新增 React 组件、页面文件或路由。
- 未新增分类 slug。
- 未修改 middleware 或代理配置。

## 同步效果

- 分类页 `rubber-products-hoses` 会自动列出该产品。
- 产品卡片、相关产品、询盘预填、SEO、canonical、hreflang、Open Graph、Product JSON-LD 均由现有数据驱动逻辑自动生成。
- 由于 `industrial-rubber-sheet` 已存在于 `products` 数组，语言切换会保持当前 slug。
