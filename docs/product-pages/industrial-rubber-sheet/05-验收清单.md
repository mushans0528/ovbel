# 05-验收清单 — Industrial Rubber Sheet / 工业橡胶板

## 数据与内容

- [x] `lib/products.ts` 中 `industrial-rubber-sheet` EN 条目已补全，字段完整。
- [x] `zhProducts["industrial-rubber-sheet"]` 中文条目已补全，与 EN 信息对等。
- [x] 未编造参数、认证、价格、库存、应用承诺。
- [x] 颜色、厚度范围、尺寸形式、SBR 参考牌号物理性能均来自 h-col-155 页面。
- [x] `sourceUrl` 指向 https://www.respowerbelt.com/h-col-155.html。

## 素材

- [x] 5 张新图片均来自 OVBEL 自有 CDN，已下载到 `public/products/industrial-rubber-sheet/`。
- [x] 图片已转码为 webp，文件命名与内容一致。
- [x] 所有图片 `alt` 文本已在中英文文案/组件中定义。
- [x] 无放大镜装饰图标。

## 页面渲染

- [x] 图库、简介、优势、应用、规格、技术文档、储运须知、询盘清单均按需展示。
- [x] 本产品无结构分层（`layers`）与花纹分支（`patterns`），故未展示 construction / patterns 区块。
- [x] 语言切换保持当前产品 slug 与查询参数。
- [x] 分类页、产品卡片、相关产品、询盘预填自动同步。

## SEO

- [x] canonical / hreflang / OG / Product JSON-LD 由现有组件自动输出。
- [x] `seo.title` / `seo.description` 已填写。
- [x] JSON-LD 不含 `offers`、`review`、`aggregateRating`。

## 构建与质量

- [x] `tsc --noEmit` 通过。
- [x] `next build` 成功，静态页面数保持预期（未新增产品 slug，仅补全现有占位条目）。
- [x] 未修改任何代理 / middleware 文件。
- [x] 文档目录 `docs/product-pages/industrial-rubber-sheet/` 下 6 份文件完整。

## 推送

- [x] 通过 GitHub Git Database API 推送到 `origin/main`。
- [x] 提交哈希、验证结果已记录。
