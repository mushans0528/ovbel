# 05 — 验收清单

> 本页对应实现与 `01–04` 文档；以下为可逐项核对的验收记录。

## A. 数据与代码
- [x] `lib/products.ts` 已新增 `anti-tear-conveyor-belt` 条目（EN + ZH）。
- [x] 未新建组件或路由；仅扩展数据模型。
- [x] `getProducts(locale)` 合并后该产品可被 EN 与 ZH 路由渲染。
- [x] `tsc --noEmit` 退出码 0，无类型错误。
- [ ] `next build` 成功（本次提交前需运行）。

## B. 页面渲染
- [ ] EN 路由 `/products/conveyor-belts/anti-tear-conveyor-belt` 正常渲染。
- [ ] ZH 路由 `/zh/products/conveyor-belts/anti-tear-conveyor-belt` 正常渲染。
- [ ] 图库（cover / belt-edge-closeup / belt-edge-detail / belt-section-samples / carcass-exploded-view）点击进入全屏灯箱，键盘 `Esc` / `←` / `→` 工作。
- [ ] 2 张技术图（belt-carcass-structure-diagram / cover-thickness-spec-table）点击全屏放大，无放大镜装饰图标。
- [ ] Patterns（4 种结构变型）正确渲染。
- [ ] Precautions（5 条）与 inquiryChecklist（6 条）正确渲染；询盘表单 `subject` 自动预填产品名。

## C. 多语言与一致性
- [ ] EN/ZH eyebrow / summary / description / applicationIntro / featureHeading 完全对齐。
- [ ] EN/ZH features（6）/ applications（8）数量一致。
- [ ] EN/ZH specifications 数量一致（4 条），术语对照见 `02-双语页面文案.md`。
- [ ] EN/ZH patterns（4）/ precautions（5）/ inquiryChecklist（6）数量与顺序一致。
- [ ] 切换语言时，URL 切换到对应 locale，产品不变。

## D. SEO / Open Graph / JSON-LD
- [ ] `<title>` 与 `seo.title` 一致（EN / ZH）。
- [ ] `<meta name="description">` 与 `seo.description` 一致。
- [ ] `<link rel="canonical">` 指向当前 locale URL。
- [ ] `hreflang` 含 `en` / `zh-CN` / `x-default`。
- [ ] `og:title` / `og:description` / `og:image` / `og:locale` / `og:type=product` 齐全；`og:image` 指向 cover。
- [ ] `Product` JSON-LD 输出，无 `offers` / `aggregateRating`。
- [ ] `manufacturer` 字段为 `Shandong Ovbel Industrial Co., Ltd.`。

## E. 媒体资源
- [x] 7 张 webp 落位于 `public/products/anti-tear-conveyor-belt/`。
- [x] 所有 `media.{cover,gallery,technicalImages}` 引用的路径在仓库内真实存在（自动校验）。
- [x] 总大小约 132 KB；最大单图 46 KB。

## F. 资料纪律
- [x] 未编造数值（拉伸强度/增强绳根数/覆盖胶厚度/温度等源页未给，未展示）。
- [x] 结构变型（钢绳/聚酯绳、单侧/双侧）由源页 Features 的明确选项推导，未添加源页未提及的类型。
- [x] 未编造价格 / 库存 / 交期 / MOQ / 保修。
- [x] 描述、优势、应用列表由源页事实编辑性改写，不照搬原句。
- [x] `sourceUrl` 标注为源页面链接。

## G. 响应式与可访问性
- [ ] 桌面端：图库与技术图排版无溢出；字号、行距、对比度满足 WCAG AA。
- [ ] 移动端：图库横向滑动；技术图等比缩放，可点击放大。
- [ ] 所有 `img` 含 `alt`（由组件内部基于产品名 + 用途生成）。
- [ ] 键盘可达：灯箱 `Esc` 关闭、左右翻页；表单字段 `Tab` 顺序合理；按钮 `:focus-visible` 样式可见。
- [ ] 颜色对比度：文本与背景满足 4.5:1（正文）/ 3:1（大字号）。

## H. 待业务确认（**不在页面展示**）
1. 带宽范围（mm） — 源页未列。
2. 带芯层数/强度等级 — 源页未给数值。
3. 横向增强绳直径/间距/根数 — 源页未给数值。
4. 覆盖胶厚度（mm）与等级 — 源页未给数值。
5. 工作温度范围 — 源页未列。
6. 最大带速（m/s） — 源页未列。
7. 最小带轮直径（mm） — 源页未列。
8. 接头方式 — 源页未列。
9. 阻燃/耐热/耐寒温度范围或认证 — 源页未列。
10. 库存 / 交期 / MOQ / 单价 / 保修 — 商业条款，源页未给。

## I. 差异检查
- [ ] 差异表显示仅 `lib/products.ts`、`public/products/anti-tear-conveyor-belt/*`、`docs/product-pages/anti-tear-conveyor-belt/*` 改动；无无关文件被修改。
- [ ] 无代理（proxy/middleware）相关文件被修改。
