# 03 — 实现规格

## 数据模型位置
- `lib/products.ts`：
  - EN 条目：`products` 数组中 `slug: "elevator-buckets"`（原占位条目位置）。
  - 中文条目：`zhProducts["elevator-buckets"]`。

## 字段映射

| 字段 | 是否使用 | 说明 |
|---|---|---|
| `slug` | 是 | `elevator-buckets` |
| `name` | 是 | EN: `Elevator Buckets` / ZH: `提升机料斗` |
| `category` | 是 | `conveyor-components` |
| `eyebrow` | 是 | 产品页 eyebrow 文案 |
| `summary` | 是 | 产品页 lead 摘要 |
| `description` | 是 | 产品简介段落 |
| `applicationIntro` | 是 | 应用区块介绍 |
| `featureHeading` | 是 | 优势区块标题 |
| `features` | 是 | 7 条优势 |
| `applications` | 是 | 5 个应用场景 |
| `specifications` | 是 | 9 项规格 |
| `precautions` | 是 | 5 条储运/安装须知 |
| `inquiryChecklist` | 是 | 6 项询盘预填清单 |
| `seo` | 是 | title / description（EN/ZH 均通过 `zhProducts` 覆盖中文 SEO） |
| `sourceUrl` | 是 | `https://www.respowerbelt.com/h-col-129.html` |
| `media` | 是 | `cover` + `gallery` + `technicalImages` |

## 媒体资源

### 图库（gallery）
1. `/products/elevator-buckets/cover.webp` — 彩色塑料料斗组合图（封面）。
2. `/products/elevator-buckets/gallery-01.webp` — 不锈钢料斗实物图（两件）。
3. `/products/elevator-buckets/gallery-02.webp` — 不锈钢料斗实物图（单件侧视）。
4. `/products/elevator-buckets/gallery-03.webp` — 不锈钢料斗实物图（单件带三孔，近景）。

### 技术图（technicalImages）
1. `/products/elevator-buckets/material-comparison-table.webp` — 材料性能对比表（温度、FDA、耐磨/抗冲击等级）。
2. `/products/elevator-buckets/material-applications-table.webp` — 材料应用场景表。
3. `/products/elevator-buckets/dimension-drawing.webp` — 料斗尺寸参数图（A/B/C/H/T）。

## 页面渲染
- 图库、简介、优势、应用、规格、技术文档、储运须知、询盘清单均按需展示。
- 本产品无结构分层（`layers`）与花纹分支（`patterns`），故不展示 construction / patterns 区块。
- 语言切换保持当前产品 slug 与查询参数。
- 分类页、产品卡片、相关产品、询盘预填、SEO、canonical、hreflang、Open Graph 和 Product JSON-LD 均通过现有组件自动输出。
