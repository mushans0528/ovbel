# 05 — 验收清单

## 数据与内容
- [ ] `lib/products.ts` 中 `elevator-buckets` EN 条目存在且字段完整。
- [ ] `zhProducts["elevator-buckets"]` 中文条目存在且与 EN 信息对等。
- [ ] 未编造参数、认证、价格、库存、应用承诺。
- [ ] 材料选项、温度参数、FDA 食品认证材料、AA/DM/EU 型号、间距与容量参考均来自 h-col-129 页面文字或图片。
- [ ] `sourceUrl` 指向 https://www.respowerbelt.com/h-col-129.html。

## 素材
- [ ] 7 张新图片均来自 OVBEL 自有 CDN，已下载到 `public/products/elevator-buckets/`。
- [ ] 图片已转码为 webp，文件命名与内容一致。
- [ ] 所有图片 `alt` 文本已在中英文文案/组件中定义。
- [ ] 无放大镜装饰图标。

## 页面渲染
- [ ] 图库、简介、优势、应用、规格、技术文档、储运须知、询盘清单均按需展示。
- [ ] 本产品无结构分层（`layers`）与花纹分支（`patterns`），故未展示 construction / patterns 区块。
- [ ] 语言切换保持当前产品 slug 与查询参数。
- [ ] 分类页、产品卡片、相关产品、询盘预填自动同步。

## SEO
- [ ] canonical / hreflang / OG / Product JSON-LD 由现有组件自动输出。
- [ ] `seo.title` / `seo.description` 已填写。
- [ ] JSON-LD 不含 `offers`、`review`、`aggregateRating`。

## 构建与质量
- [ ] `tsc --noEmit` 通过。
- [ ] `next build` 成功，静态页面数保持预期（未新增产品 slug，仅补全现有占位条目）。
- [ ] 未修改任何代理 / middleware 文件。
- [ ] 文档目录 `docs/product-pages/elevator-buckets/` 下 6 份文件完整。

## 推送
- [ ] 通过 GitHub Git Database API 推送到 `origin/main`。
- [ ] 提交哈希、验证结果已记录。

## 仍待业务确认（未在页面展示）
- 12 种以上料斗型号的完整命名、结构图与尺寸表。
- 每种材料的具体牌号/等级（如不锈钢 304/316、HDPE 食品级牌号、尼龙具体规格）。
- FDA 认证的具体标准号与适用范围。
- 料斗尺寸 A/B/C/H/T 的具体数值表。
- 安装孔直径、孔数、背板厚度及可定制范围。
- 单斗容量、推荐带速、最大允许块度。
- 库存、交期、MOQ、价格、包装方式与保修。
