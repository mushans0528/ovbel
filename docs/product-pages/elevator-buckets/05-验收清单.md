# 05 — 验收清单

## 数据与内容
- [x] `lib/products.ts` 中 `elevator-buckets` EN 条目存在且字段完整。
- [x] `zhProducts["elevator-buckets"]` 中文条目存在且与 EN 信息对等。
- [x] 未编造参数、认证、价格、库存、应用承诺。
- [x] 材料选项、温度参数、FDA 食品认证材料、AA/DM/EU 型号、间距与容量参考均来自 h-col-129 页面文字或图片。
- [x] `sourceUrl` 指向 https://www.respowerbelt.com/h-col-129.html。
- [x] 本次 v2 图片替换未触文案/spec/inquiry/SEO 字段，仅更新 `media`。

## 素材
- [x] 4 张详情页图库图片全部由用户提供（OVBEL 自有产品素材）：`1.jpg`、`2.png`、`3.jpg`、`4.jpg`。
- [x] 已转码为 webp，文件命名与 1→2→3→4 顺序一致（`cover` + `gallery-01/02/03`）。
- [x] EN 与 ZH 各提供 4 条 `galleryAlt` 文本，描述每张图的内容（材料、形状、视角）。
- [x] 技术图（`material-comparison-table`、`material-applications-table`、`dimension-drawing`）保留不动；版权与来源沿用 v1。
- [x] 无放大镜装饰图标。

## 页面渲染
- [x] 图库、简介、优势、应用、规格、技术文档、储运须知、询盘清单均按需展示。
- [x] 本产品无结构分层（`layers`）与花纹分支（`patterns`），故未展示 construction / patterns 区块。
- [x] 语言切换保持当前产品 slug 与查询参数。
- [x] 分类页、产品卡片、相关产品、询盘预填自动同步。

## SEO
- [x] canonical / hreflang / OG / Product JSON-LD 由现有组件自动输出。
- [x] `seo.title` / `seo.description` 已填写。
- [x] JSON-LD 不含 `offers`、`review`、`aggregateRating`。

## 构建与质量
- [x] `tsc --noEmit` 通过（0 错误）。
- [x] `next build` 成功，静态页面数 32（产品已存在，本次仅替换图库，未新增页）。
- [x] 未修改任何代理 / middleware 文件。

## Diff 摘要（v1 → v2）
- EN `media.gallery`：3 项 → 4 项（首位补 `cover`）；新增 `galleryAlt` 4 条。
- ZH 增量：原 zhProducts 仅有 `sourceUrl`，v2 补齐 `media` 块（4 项 gallery + 4 条 ZH `galleryAlt` + 3 项 `technicalImages`）。
- 图片资产：`cover.webp` 与 `gallery-01.webp` / `gallery-02.webp` / `gallery-03.webp` 4 个文件名被覆盖为 OVBEL 自有素材；技术图 3 张保留。
- 文档：本文件与 `04-素材与SEO规范.md`、`README.md` 按 v1→v2 重写。

## 推送
- [x] `git push`（PAT 复用第 6 次，请尽快在 https://github.com/settings/tokens 中 Revoke 后换发）。

## 仍待业务确认（未在页面展示）
- 12 种以上料斗型号的完整命名、结构图与尺寸表。
- 每种材料的具体牌号/等级（如不锈钢 304/316、HDPE 食品级牌号、尼龙具体规格）。
- FDA 认证的具体标准号与适用范围。
- 料斗尺寸 A/B/C/H/T 的具体数值表。
- 安装孔直径、孔数、背板厚度及可定制范围。
- 单斗容量、推荐带速、最大允许块度。
- 库存、交期、MOQ、价格、包装方式与保修。
