# 03 - 实现规格

## 数据位置

- **英文源数据**: `lib/products.ts` 中 `products` 数组，位于 `polyurethane-belt-cleaner` 之后、`elevator-buckets` 之前。
- **中文数据**: `lib/products.ts` 中 `zhProducts` 对象，键为 `"conveyor-belt-cover"`，位于 `polyurethane-belt-cleaner` 之后、`elevator-buckets` 之前。

## 复用架构

- 未新增任何页面组件或路由。
- 使用现有 `ProductDetailPage`、`ProductGallery`、`ZoomableImage`（无放大镜装饰图标）渲染。
- 分类页、产品卡片、相关产品、询盘预填、SEO、canonical、hreflang、Open Graph、Product JSON-LD 均通过 `getProducts(locale)` 数据驱动自动生成。

## 字段映射

| 页面区块 | 数据来源字段 |
|----------|--------------|
| 顶部 eyebrow / name / summary | `eyebrow`, `name`, `summary` |
| 主图 / 图库 | `media.cover`, `media.gallery` |
| 简介 | `description` |
| 应用说明 | `applicationIntro` |
| 优势 | `featureHeading`, `features[]` |
| 应用 | `applications[]` |
| 规格 | `specifications[]` |
| 储运 | `precautions[]` |
| 询盘清单 | `inquiryChecklist[]` |
| 相关产品 | 同分类其他产品自动推荐 |
| SEO / OG / JSON-LD | `seo.title`, `seo.description`, `name`, `description` |

## 新增媒体

- `public/products/conveyor-belt-cover/cover.webp`
- `public/products/conveyor-belt-cover/gallery-02.webp`
- `public/products/conveyor-belt-cover/gallery-03.webp`
- `public/products/conveyor-belt-cover/gallery-04.webp`
- `public/products/conveyor-belt-cover/spec-table.webp`

## 构建验证

- `tsc --noEmit`: 0 errors
- `next build`: 38 static pages generated (previous: 37)
