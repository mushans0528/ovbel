# 05 - 验收清单（Endless Conveyor Belt）

> ✅ = 已实现/已验证；⬜ = 待业务确认或待处理

## A. 内容事实（要求 2）

- ✅ 名称、特性、应用、规格、织物代号、型号均来自原站明示内容，未编造。
- ✅ 原站未提供标准/认证/价格/库存 → 规格中未编造标准字段，相关项标记为待确认。
- ✅ 数据分级（已确认 / 编辑性 / 待确认）在 `01-原站内容审计.md` 可追溯矩阵中可查。

## B. 多语言（要求 5）

- ✅ EN/ZH 信息量一致：特性 6、应用 7、规格 15、储运 4、询盘 7 一一对应。
- ✅ 术语准确（EP/TC/NN/EPP/CC/XE 等保留国际代号并附中英文含义）。
- ✅ 语言切换保留当前产品路径与查询参数（SiteHeader 既有逻辑）。

## C. 页面与交互（要求 4/7/8）

- ✅ 板块按需展示：图库、简介、优势、应用、规格、织法（型号/花纹）、储运、询盘清单、相关产品。
- ✅ 图片点击全屏放大（灯箱），键盘 Esc/←/→ 可操作。
- ✅ 灯箱**无放大镜装饰图标**。
- ✅ 响应式（桌面 + 移动端，沿用既有组件）。
- ✅ alt 文本已补全（EN/ZH 各 4 条）。

## D. SEO（要求 6）

- ✅ 分类页、产品卡片、相关产品经数据模型自动同步。
- ✅ 询盘经 `?product=endless-conveyor-belt` 自动预填。
- ✅ `canonical` / `hreflang`(en, zh-CN, x-default) / Open Graph / Product JSON-LD 由 `generateMetadata` 自动生成。
- ✅ SEO title/description 已分别提供 EN/ZH。

## E. 素材（要求 7）

- ✅ 4 张产品图全部使用**用户提供的 OVBEL 自有素材**（`cover` + `gallery-01/02/03`），由源 PNG 转 webp。
- ✅ 第三方版权风险已消除（v1 来自 sjbelt.com 的素材已全部替换）。

## F. 技术（要求 1/3/9）

- ✅ 未新建重复架构，仅扩展 `lib/products.ts`（EN + ZH）。
- ✅ `npx tsc --noEmit` 通过。
- ✅ `npm run build` 通过（35 静态页）。
- ✅ `git diff --stat` 仅含预期文件（`lib/products.ts` + `public/products/endless-conveyor-belt/` + `docs/product-pages/endless-conveyor-belt/`）。
- ℹ️ 既有 `middleware`→`proxy` 废弃警告仍存在（Next.js 16 预置，非本次引入，按「不修改代理设置」要求保留）。

## G. 业务签核（待业务确认）

- ✅ 已替换第三方版权图片为 OVBEL 自有素材（用户直接提供 4 张）。
- ⬜ 补充产品标准/认证（如 GB/T、DIN、ISO、CE 等）。
- ⬜ 确认适用温度范围、最小 pulley 直径、安全系数。
- ⬜ 确认储运细则（当前为通用建议）。
- ⬜ 确认价格、库存、MOQ 口径（如需在页面展示）。
- ⬜ 确认型号命名（高强度型/标准型）与内部料号映射。
- ⬜ 可选：补充剖面图、应用场景图等丰富素材（见 04 §4）。

## H. 提交与推送（要求 11）

- ⬜ `git add` 预期文件 → `commit`（feat(endless-conveyor-belt) 风格）。
- ⬜ 推送到 `origin/main`（使用一次性 PAT，不改动代理设置）。
- ⬜ 报告提交哈希、验证结果与本清单「待业务确认」项。
