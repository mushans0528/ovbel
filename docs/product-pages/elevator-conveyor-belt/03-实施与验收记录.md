# 实施与验收记录：Elevator Conveyor Belt

日期：2026-08-09

## 代码修改

| 文件 | 修改内容 |
| --- | --- |
| `lib/products.ts` | 英文 `products` 与 `zhProducts` 各新增 `elevator-conveyor-belt` 条目（含 media.gallery） |
| `app/[locale]/[[...path]]/page.tsx` | ① 产品数量文案 12 → 13（首页统计、目录标题，双语）；② `generateMetadata` 增加页面级 Open Graph（title/description/url，产品页含 gallery 图片） |
| `README.md` | 产品详情页数量 Twelve → Thirteen |
| `.gitignore` | 增加 `*.tsbuildinfo` |
| `public/products/elevator-conveyor-belt/` | 新增 4 张原站产品图片（WebP，描述性命名） |
| `docs/product-pages/elevator-conveyor-belt/` | 本产品档案、内容审计、双语文案、本记录 |

复用统一架构：`ProductDetailPage` / `ProductGallery` / `ProductCard` / 询盘预填 / SEO 生成，未新建任何页面或组件。

## 同步区域检查

- 产品中心、输送带分类页：自动出现新产品（已验证中文分类页）
- 相关产品：同分类（花纹输送带、粗面输送带）自动联动
- 询盘预填：`?product=elevator-conveyor-belt` 显示本地化产品名与专属清单（已验证中文）
- 语言切换：沿用 `SiteHeader` 现有逻辑，保留当前路径与查询参数
- 首页精选取前 6 款产品，新产品位于输送带分类第 2 位，自然进入精选

## 验证结果

| 项目 | 结果 |
| --- | --- |
| `tsc --noEmit` | 通过，0 错误 |
| `next build`（生产构建） | 通过，29 个静态页面生成成功 |
| 英文产品页 | 200，`/en/products/conveyor-belts/elevator-conveyor-belt` |
| 中文产品页 | 200，`/zh/products/conveyor-belts/elevator-conveyor-belt` |
| 无效 slug | 404 |
| 无效语言前缀 | 中间件 307 至 `/en/...` 后由应用返回 404（既有行为，未改动） |
| canonical / hreflang | 当前语言 canonical；en / zh-CN / x-default 齐全 |
| Open Graph | 产品级 og:title / description / url / 4 张 og:image（本次修复） |
| Product JSON-LD | 名称、摘要、分类、品牌、制造商、URL、4 张图片；无价格/库存/评分字段 |
| 图片 | 4 张均可访问（200），图库点击全屏放大，无放大镜装饰图标 |
| 键盘与无障碍 | 沿用共享组件：灯箱 Esc 关闭、方向键切换、打开后焦点移至关闭按钮、aria 标签完整 |

## 未决事项（待业务确认）

见 `01-原站内容审计.md` 第三节：ATEX 证书范围、ISO 284 检测报告、打孔能力现状、堆密度参考值口径、型谱参数补充、中文惯用名。

## 提交信息

- 提交哈希：见 git log（feat 提交与 docs 补记提交）
- 推送目标：origin/main
- 发布属于外部操作，需由有部署权限的人员执行（opennextjs-cloudflare deploy）
