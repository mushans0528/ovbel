# 05 — 验收清单

## 数据与内容
- [x] `lib/products.ts` 中新增 `polyurethane-belt-cleaner` EN 条目，字段完整。
- [x] `zhProducts` 中新增对应中文条目，与 EN 信息对等。
- [x] 未编造参数、认证、价格、库存、应用承诺。
- [x] 数字规格（带宽、带速、温度、湿度）来自原页文字。
- [x] 选型表以技术图片保留，未进行不可溯源的逐格转录。
- [x] `sourceUrl` 指向 https://www.respowerbelt.com/h-col-126.html。

## 素材
- [x] 5 张图片均来自 OVBEL 自有 CDN，已下载到 `public/products/polyurethane-belt-cleaner/`。
- [x] 图片已转码为 webp，文件命名与内容一致。
- [x] 所有图片 `alt` 文本已在中英文文案中定义。
- [x] 无放大镜装饰图标。

## 页面渲染
- [x] 图库、简介、优势、应用、规格、储运、询盘清单均按需展示。
- [x] 本产品为单一型号，无花纹/系列分支，故未展示“型号/花纹”区块。
- [x] 语言切换保持当前产品 slug 与查询参数。
- [x] 分类页、产品卡片、相关产品、询盘预填自动同步。

## SEO
- [x] canonical / hreflang / OG / Product JSON-LD 由现有组件自动输出。
- [x] `seo.title` / `seo.description` 已填写。
- [x] JSON-LD 不含 `offers`、`review`、`aggregateRating`。

## 构建与质量
- [x] `tsc --noEmit` 通过（0 错误，在清理 `.next` 生成类型后执行）。
- [x] `next build` 成功，静态页面数增加。
- [x] 未修改任何代理 / middleware 文件。
- [x] 文档目录 `docs/product-pages/polyurethane-belt-cleaner/` 下 6 份文件完整。

## 推送
- [x] 通过 GitHub Git Database API 推送到 `origin/main`。
- [x] 提交哈希、验证结果已记录。

## 仍待业务确认（未在页面展示）
- 聚氨酯刮刀具体硬度 / 材质牌号 / 颜色选项。
- 单段刮刀尺寸与重量。
- 支撑管、支架、弹簧的材质与规格。
- 适用认证（CE、ATEX、MSHA 等）。
- 选型表上限（约 3000 mm）与页面文字上限（2400 mm）的一致性。
- 库存、交期、MOQ、价格、保修。
