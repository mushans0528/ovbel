# 05 — 验收清单

## 数据与内容
- [x] `lib/products.ts` 中新增 `steel-cord-conveyor-belt` EN 条目，字段完整。
- [x] `zhProducts` 中新增对应中文条目，与 EN 信息对等。
- [x] 未编造参数、认证、价格、库存、应用承诺。
- [x] 数值型规格仅通过 `technicalImages` 图片展示。
- [x] 8 种覆盖胶类型来自原站事实。
- [x] `sourceUrl` 指向 https://www.respowerbelt.com/h-col-136.html。

## 素材
- [x] 8 张图片均来自 OVBEL 自有 CDN，已下载到 `public/products/steel-cord-conveyor-belt/`。
- [x] 图片已转码为 webp，文件命名与内容一致。
- [x] 所有图片 `alt` 文本已在中英文文案中定义。
- [x] 无放大镜装饰图标。

## 页面渲染
- [x] 图库、简介、优势、应用、规格、型号/花纹、储运、询盘清单均按需展示。
- [x] 语言切换保持当前产品 slug 与查询参数。
- [x] 分类页、产品卡片、相关产品、询盘预填自动同步。

## SEO
- [x] canonical / hreflang / OG / Product JSON-LD 由现有组件自动输出。
- [x] `seo.title` / `seo.description` 已填写。
- [x] JSON-LD 不含 `offers`、`review`、`aggregateRating`。

## 构建与质量
- [x] `tsc --noEmit` 通过（0 错误）。
- [x] `next build` 成功，静态页面数增加 1。
- [x] 未修改任何代理 / middleware 文件。
- [x] 文档目录 `docs/product-pages/steel-cord-conveyor-belt/` 下 6 份文件完整。

## 推送
- [x] 通过 GitHub Git Database API 推送到 `origin/main`。
- [x] 提交哈希、验证结果已记录。

## 仍待业务确认（未在页面展示）
- 具体带宽范围、带长范围。
- ST 强度等级默认/可选范围。
- 钢丝绳根数、间距、直径默认配置。
- 覆盖胶厚度 t1/t2 具体值。
- 工作温度范围（耐热/耐寒具体温度）。
- 最大带速、最小带轮直径、接头方式。
- 阻燃/耐热/耐寒认证细节。
- 库存、交期、MOQ、价格、保修。
