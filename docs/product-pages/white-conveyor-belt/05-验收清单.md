# 05 — 验收清单

> 本页对应实现与 `01–04` 文档；以下为可逐项核对的验收记录。

## A. 数据与代码
- [x] `lib/products.ts` 已扩展 EN + ZH 条目，slug=`white-conveyor-belt`、category=`conveyor-belts`。
- [x] 未新建组件或路由；仅扩展数据模型。
- [x] `getProducts(locale)` 合并后白带产品可被 EN 与 ZH 路由渲染。
- [x] `tsc --noEmit` 退出码 0，无类型错误。
- [ ] `next build` 成功（本次提交前需运行）。

## B. 页面渲染
- [ ] EN 路由 `/products/conveyor-belts/white-conveyor-belt` 正常渲染。
- [ ] ZH 路由 `/zh/products/conveyor-belts/white-conveyor-belt` 正常渲染。
- [ ] 图库（cover / factory-overview / production-line / belt-detail）点击进入全屏灯箱，键盘 `Esc` / `←` / `→` 工作。
- [ ] 规格表（spec-table）点击全屏放大，无放大镜装饰图标。
- [ ] Layers（3 层）正确渲染于规格/结构区。
- [ ] Precautions（5 条）正确渲染。
- [ ] inquiryChecklist（6 条）正确渲染；询盘表单 `subject` 自动预填产品名。

## C. 多语言与一致性
- [ ] EN/ZH eyebrow / summary / description / applicationIntro / featureHeading 完全对齐。
- [ ] EN/ZH features / applications 数量一致（中英文各 6 / 5）。
- [ ] EN/ZH specifications 数量一致（13 项），术语对照表见 `02-双语页面文案.md`。
- [ ] EN/ZH layers / precautions / inquiryChecklist 数量与顺序一致。
- [ ] 切换语言时，URL 切换到对应 locale，产品不变。

## D. SEO / Open Graph / JSON-LD
- [ ] `<title>` 与 `seo.title` 一致（EN / ZH）。
- [ ] `<meta name="description">` 与 `seo.description` 一致。
- [ ] `<link rel="canonical">` 指向当前 locale URL。
- [ ] `hreflang` 含 `en` / `zh-CN` / `x-default`。
- [ ] `og:title` / `og:description` / `og:image` / `og:locale` / `og:type=product` 齐全；`og:image` 指向 cover。
- [ ] `Product` JSON-LD 输出，无 `offers` / `aggregateRating`。
- [ ] `manufacturer` 字段为 `Shandong Ovbel Industrial Co., Ltd.`。

## E. 媒体资源
- [x] 5 张 webp 落位于 `public/products/white-conveyor-belt/`。
- [x] 所有 `media.{cover,gallery,technicalImages}` 引用的路径在仓库内真实存在（自动校验通过）。
- [x] 总大小约 450 KB；最大单图 ≤ 250 KB。

## F. 资料纪律
- [x] 未编造参数（13 项规格全部源自源页规格图，逐字对齐）。
- [x] 未编造认证（FDA / EU / GB 等均未在页面展示）。
- [x] 未编造价格 / 库存 / 交期 / MOQ / 保修。
- [x] 描述、优势、应用列表由源页事实编辑性改写，不照搬原句。
- [x] sourceUrl 标注为源页面链接。

## G. 响应式与可访问性
- [ ] 桌面端：图库与规格表并列排版时无溢出；字号、行距、对比度满足 WCAG AA。
- [ ] 移动端：图库横向滑动；规格表等比缩放，可点击放大。
- [ ] 所有 `img` 含 `alt`（由组件内部基于产品名 + 用途生成）。
- [ ] 键盘可达：灯箱 `Esc` 关闭、左右翻页；表单字段 `Tab` 顺序合理；按钮 `:focus-visible` 样式可见。
- [ ] 颜色对比度：文本与背景满足 4.5:1（正文）/ 3:1（大字号）。

## H. 待业务确认（**不在页面展示，仅在交付文档与提交报告中列明**）
1. 食品接触认证（FDA / EU 1935/2004 / GB 4806 等） — 源页未提。
2. 工作温度下限 — 源页仅给上限 80 °C。
3. 带宽范围 — 源页未列。
4. 带芯结构选项代号 — 源页未列。
5. 覆盖胶厚度 — 源页未列。
6. 最小带轮直径 — 源页未列。
7. 接头方式 — 源页未列。
8. 颜色选项 — 仅白色，不声称其他颜色。
9. 库存 / 交期 / MOQ / 单价 / 保修 — 商业条款，源页未给。
10. 倾斜输送能力 — 源页未给倾角数据。

## I. 差异检查
- [ ] `git diff --stat`（或本仓库对应的 API 差异表）显示仅 `lib/products.ts`、`public/products/white-conveyor-belt/*`、`docs/product-pages/white-conveyor-belt/*` 改动；无无关文件被修改。
- [ ] 无代理（proxy）相关文件被修改。