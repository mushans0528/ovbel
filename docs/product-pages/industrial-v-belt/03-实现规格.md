# 03 - 实现规格

## 复用架构

- 分类：`power-transmission-belts`（已存在，未新建）
- slug：`industrial-v-belt`（仓库中已存在 stub，本次充实内容，未新建 slug/路由/组件）
- 路由：`/[locale]/products/[category]/[slug]` 数据驱动，自动接入 EN/ZH 页面
- 组件：`ProductDetailPage`、`ProductGallery`、`ZoomableImage`、`ProductCard`、`InquiryForm`

## 数据模型变更

文件：`lib/products.ts`

- EN `products[]`：将原有 `industrial-v-belt` stub 替换为完整字段：
  - `name`, `eyebrow`, `summary`, `description`
  - `featureHeading`, `features`, `applications`
  - `specifications`
  - `precautions`, `inquiryChecklist`
  - `seo: { title, description }`
  - `sourceUrl`
  - `media: { cover, gallery[], technicalImages[] }`
- ZH `zhProducts{}`：同步替换同名 slug 的中文内容，字段与 EN 一致。

## 图片文件

目录：`public/products/industrial-v-belt/`

- `cover.webp` — 多根包布 V 带组合图
- `gallery-01.webp` — 多楔/多肋带局部特写
- `gallery-02.webp` — 单根包布 V 带盘绕图
- `specification-table.webp` — 规格参数表（原站截图）

所有图片由 CDN 原图经 Pillow 转为 WebP（quality 92），照片 max 1600px，规格表 max 2400px。

## 自动级联效果

- 分类页 `/products/power-transmission-belts` 自动显示更新后的产品卡片。
- 相关产品：同分类其他产品（timing-belt、transmission-flat-belt）自动出现在详情页底部。
- 询盘页 `/request-quote?product=industrial-v-belt` 自动预填产品名与询盘清单。
- SEO：canonical、hreflang（en / zh-CN / x-default）、Open Graph、Product JSON-LD 自动生成。
