# 03 — 实现规格

## 数据模型位置

- `lib/products.ts`：
  - EN 条目：`products` 数组中新增 `slug: "timing-belt"`（位于 `industrial-v-belt` 之后、`impact-bed` 之前），分类为 `power-transmission-belts`。
  - 中文条目：`zhProducts["timing-belt"]`（与 `industrial-v-belt` 等动力传动带相邻）。

## 字段映射

| 字段 | 是否使用 | 说明 |
|---|---|---|
| `slug` | 是 | `timing-belt` |
| `name` | 是 | EN: `Timing Belt` / ZH: `同步带` |
| `category` | 是 | `power-transmission-belts` |
| `eyebrow` | 是 | 产品页 eyebrow 文案 |
| `summary` | 是 | 产品页 lead 摘要 |
| `description` | 是 | 产品简介段落（含三层结构说明） |
| `applicationIntro` | 是 | 应用区块引导语 |
| `featureHeading` | 是 | 优势区块标题 |
| `features` | 是 | 8 条优势 |
| `applications` | 是 | 6 个应用场景（编辑性归纳，待业务确认） |
| `specifications` | 是 | 9 项规格 |
| `precautions` | 是 | 5 条储运/安装须知 |
| `inquiryChecklist` | 是 | 6 项询盘预填清单 |
| `seo` | 是 | title / description（EN/ZH 均通过 `zhProducts` 覆盖中文 SEO） |
| `sourceUrl` | 是 | `https://www.respowerbelt.com/h-col-156.html` |
| `media` | 是 | `cover` + `gallery`（5 张）+ `technicalImages`（1 张） |
| `layers` | 否 | 共享组件的 construction 区块文案为缓冲床定制，本产品不使用，避免显示错误文案；结构信息已并入 `description` 与 `specifications` |
| `patterns` | 否 | 本产品未设花纹分支 |

## 媒体资源

### 图库（gallery）

1. `/products/timing-belt/cover.webp` — 同步带产品图（封面，同时用于产品卡片）。
2. `/products/timing-belt/gallery-01.webp` — 同步带产品图。
3. `/products/timing-belt/gallery-02.webp` — 同步带产品图。
4. `/products/timing-belt/gallery-03.webp` — 同步带产品图。
5. `/products/timing-belt/banner.webp` — 产品横幅图（原透明 PNG 已合成白底）。

### 技术图（technicalImages）

1. `/products/timing-belt/specification-diagram.webp` — 同步带结构 / 尺寸示意图。

## 页面渲染

- 图库、简介、应用、规格、技术文档、优势、储运须知、询盘清单均按需展示。
- 本产品无 `patterns` 分支，故不展示 patterns 区块；未使用 `layers`，故不展示 construction 区块（避免缓冲床定制文案）。
- 语言切换保持当前产品 slug 与查询参数。
- 分类页、产品卡片、相关产品、询盘预填、SEO、canonical、hreflang、Open Graph 和 Product JSON-LD 均通过现有组件自动输出。
- 新增产品 slug 自动生成 `/products/power-transmission-belts/timing-belt`（en）与 `/zh/products/power-transmission-belts/timing-belt`（zh）路由。
