# 03 — 实现规格

> 与现有 OVBEL 产品详情页实现保持一致，仅扩展 `lib/products.ts` 的数据模型；未新增组件、未新增路由。

## 1. 数据模型扩展

### 1.1 变更对象
- `lib/products.ts`：**新增** `polyester-conveyor-belt` 条目（EN，位于 `white-conveyor-belt` 之后、`transmission-flat-belt` 之前）+ `zhProducts["polyester-conveyor-belt"]`（ZH，zhProducts 末尾）。

### 1.2 字段填充
| 字段 | 是否填充 | 来源 |
|---|---|---|
| `slug` / `name` / `category` | ✓ | `polyester-conveyor-belt` / Polyester Conveyor Belt / 聚酯芯输送带 / `conveyor-belts` |
| `eyebrow` | ✓ | Multi-ply EP carcass / 多层 EP 带芯 |
| `summary` | ✓ | 编辑性改写 |
| `description` | ✓ | 编辑性改写（围绕源页定位） |
| `applicationIntro` | ✓ | 编辑性改写 |
| `featureHeading` | ✓ | 编辑性改写 |
| `features` | ✓ | 6 条（围绕源页特性/应用/标准） |
| `applications` | ✓ | 6 条（源自源页 Application 2 点展开） |
| `specifications` | ✓ | 4 条（带芯/覆盖胶/参照标准/运行条件） |
| `precautions` | ✓ | 5 条（通用橡胶带储运/安装实践） |
| `inquiryChecklist` | ✓ | 6 条（物料/尺寸/覆盖胶/温度/带速/数量） |
| `seo` | ✓ | title + description |
| `sourceUrl` | ✓ | `https://www.respowerbelt.com/h-col-122.html` |
| `media` | ✓ | cover + gallery(4) + technicalImages(1) |
| `patterns` | ✗ | 留空（源页无型号/花纹数据） |
| `layers` | ✗ | 留空（源页未给层结构数值） |

### 1.3 `media` 结构
```ts
media: {
  cover: "/products/polyester-conveyor-belt/cover.webp",
  gallery: [
    "/products/polyester-conveyor-belt/cover.webp",
    "/products/polyester-conveyor-belt/belt-roll.webp",
    "/products/polyester-conveyor-belt/construction.webp",
    "/products/polyester-conveyor-belt/belt-detail.webp",
  ],
  technicalImages: [
    "/products/polyester-conveyor-belt/standard-list.webp",
  ],
}
```

## 2. 组件复用
| 组件 | 用途 | 是否修改 |
|---|---|---|
| `ProductDetailPage` | 详情页主框架，数据驱动 | 否 |
| `ProductGallery` | 主图库（点击全屏灯箱，键盘 Esc/左右） | 否 |
| `ZoomableImage` | 标准/参数表等内嵌图的点击放大（无放大镜图标） | 否 |
| `ProductCard` | 卡片视图（首页、分类页、相关产品） | 否 |
| `InquiryForm` | 询盘表单，自动预填产品名与询盘条目 | 否 |

## 3. 路由与 SEO
- 详情页路由：`/products/conveyor-belts/polyester-conveyor-belt`（en）/ `/zh/products/conveyor-belts/polyester-conveyor-belt`（zh），由既有通用路由处理，无需修改。
- `canonical` / `hreflang`（en, zh-CN, x-default）/ `Open Graph` / `Product JSON-LD` 由既有组件基于 `seo` 自动输出。
- 不含 `offers` / `aggregateRating`（不虚构价格/库存/评分）。

## 4. 多语言与查询参数
- EN/ZH 信息量一致（eyebrow/summary/description/applicationIntro/featureHeading/features/applications/specifications/precautions/inquiryChecklist/seo 全部对齐，数量一致）。
- 语言切换保持当前产品 slug 与询盘条目（既有 `getProducts(locale)` 实现）。

## 5. 关联产品
- 同一分类（`conveyor-belts`）其它产品（chevron / elevator / rough-top / sidewall / solid-woven / white）自动作为"相关产品"渲染。

## 6. 图片素材落位
- 全部位于 `public/products/polyester-conveyor-belt/`：
  - `cover.webp` (903×628, 51 KB)
  - `belt-roll.webp` (489×448, 16 KB)
  - `construction.webp` (608×383, 10 KB)
  - `belt-detail.webp` (400×356, 10 KB)
  - `standard-list.webp` (503×198, 6 KB)

## 7. 文档与制品
- 同步在 `docs/product-pages/polyester-conveyor-belt/` 落 6 份文档（README + 审计/文案/实现/素材/验收）。