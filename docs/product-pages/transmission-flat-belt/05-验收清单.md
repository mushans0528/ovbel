# 05 — 验收清单

> 本页对应实现与 `01–04` 文档；以下为可逐项核对的验收记录。

## A. 数据与代码
- [x] `lib/products.ts` 已扩展既有 `transmission-flat-belt` 条目（EN + ZH），未新增重复产品。
- [x] 未新建组件或路由；仅扩充数据模型。
- [x] `getProducts(locale)` 合并后该产品可被 EN 与 ZH 路由渲染。
- [x] `tsc --noEmit` 退出码 0，无类型错误。
- [ ] `next build` 成功（本次提交前需运行）。

## B. 页面渲染
- [ ] EN 路由 `/products/power-transmission-belts/transmission-flat-belt` 正常渲染。
- [ ] ZH 路由 `/zh/products/power-transmission-belts/transmission-flat-belt` 正常渲染。
- [ ] 图库（cover / cut-edge / round-edge / belt-roll）点击进入全屏灯箱，键盘 `Esc` / `←` / `→` 工作。
- [ ] 盎司等级表（ounce-grades-table）点击全屏放大，无放大镜装饰图标。
- [ ] Patterns（切边平带 / 圆边平带）正确渲染。
- [ ] Precautions（5 条）与 inquiryChecklist（6 条）正确渲染；询盘表单 `subject` 自动预填产品名。

## C. 多语言与一致性
- [ ] EN/ZH eyebrow / summary / description / applicationIntro / featureHeading 完全对齐。
- [ ] EN/ZH features / applications 数量一致（各 6 条）。
- [ ] EN/ZH specifications 数量一致（5 条），术语对照见 `02-双语页面文案.md`。
- [ ] EN/ZH patterns / precautions / inquiryChecklist 数量与顺序一致。
- [ ] 切换语言时，URL 切换到对应 locale，产品不变。

## D. SEO / Open Graph / JSON-LD
- [ ] `<title>` 与 `seo.title` 一致（EN / ZH）。
- [ ] `<meta name="description">` 与 `seo.description` 一致。
- [ ] `<link rel="canonical">` 指向当前 locale URL。
- [ ] `hreflang` 含 `en` / `zh-CN` / `x-default`。
- [ ] `og:title` / `og:description` / `og:image` / `og:locale` / `og:type=product` 齐全；`og:image` 指向 cover。
- [ ] `Product` JSON-LD 输出，无 `offers` / `aggregateRating`。
- [ ] `manufacturer` 字段为 `Shandong Ovbel Industrial Co., Ltd.`。

## E. 媒体资源
- [x] 5 张 webp 落位于 `public/products/transmission-flat-belt/`（v2：cover + gallery-01/02/03 + 沿用的 ounce-grades-table）。
- [x] 所有 `media.{cover,gallery,technicalImages}` 引用的路径在仓库内真实存在（自动校验）。
- [x] 总大小约 290 KB（v2：30 + 47 + 111 + 85 + 18）；最大单图 ≈111 KB（gallery-02）。
- [x] 4 张图来源于 OVBEL 自有实拍素材（用户于 2026-08-30 提供），**无第三方版权风险**（v1 的 `RESPOWER` 水印风险已消除）。

## F. 资料纪律
- [x] 未编造参数（28–36 OZ、棉帆布、切边/圆边结构说明全部源自源页）。
- [x] 未编造认证 / 价格 / 库存 / 交期 / MOQ / 保修。
- [x] 描述、优势、应用列表由源页事实编辑性改写，不照搬原句。
- [x] `sourceUrl` 标注为源页面链接。

## G. 响应式与可访问性
- [ ] 桌面端：图库与规格表排版无溢出；字号、行距、对比度满足 WCAG AA。
- [ ] 移动端：图库横向滑动；盎司等级表等比缩放，可点击放大。
- [ ] 所有 `img` 含 `alt`（由组件内部基于产品名 + 用途生成）。
- [ ] 键盘可达：灯箱 `Esc` 关闭、左右翻页；表单字段 `Tab` 顺序合理；按钮 `:focus-visible` 样式可见。
- [ ] 颜色对比度：文本与背景满足 4.5:1（正文）/ 3:1（大字号）。

## H. 待业务确认（**不在页面展示**）
1. 带宽范围（mm） — 源页未列。
2. 最大带速（m/s） — 源页未列。
3. 各 OZ 等级对应的额定功率/拉力 — 源页仅列等级代号，无数值表。
4. 厚度与层数（ply） — 源页未列。
5. 最小带轮直径 — 源页未列。
6. 接头方式 — 源页未列。
7. 颜色选项 — 源页未列。
8. 库存 / 交期 / MOQ / 单价 / 保修 — 商业条款，源页未给。

## I. 差异检查
- [ ] 差异表显示仅 `lib/products.ts`、`public/products/transmission-flat-belt/*`（v1 3 张 webp 删除 + v2 4 张 webp 新增）、`docs/product-pages/transmission-flat-belt/*` 改动；无无关文件被修改。
- [ ] 无代理（proxy/middleware）相关文件被修改。
- [ ] v2 资产替换已落地：图库 `cover / gallery-01 / gallery-02 / gallery-03` 来自用户提供的 OVBEL 自有 PNG（按 1→2→3→4 命名映射），`ounce-grades-table.webp` 沿用 v1。